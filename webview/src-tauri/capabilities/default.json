{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Main window only — this shell intentionally exposes no IPC capabilities; the hosted web app is fully self-contained.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
